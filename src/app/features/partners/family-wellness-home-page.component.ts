import { ChangeDetectionStrategy,Component,inject,signal } from '@angular/core';import { RouterLink } from '@angular/router';import { PartnerApiService } from '../../core/services/partner-api.service';@Component({selector:'app-family-wellness-home',imports:[RouterLink],template:`<main class="mx-auto max-w-5xl px-5 py-10"><div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-sm font-bold uppercase tracking-widest text-brand-700">Healthy Families</p><h1 class="mt-2 text-4xl font-black text-slate-950">My Family</h1><p class="mt-2 text-slate-600">One place for family wellness, care access and programme benefits.</p></div><a routerLink="/me/family" class="rounded-xl border border-slate-300 px-4 py-2 font-bold">Manage family members</a></div>@if(loading()){<p class="mt-8">Loading your family programmes…</p>}@else if(!programmes().length){<section class="mt-8 rounded-3xl border border-slate-200 bg-white p-7"><h2 class="text-2xl font-black">Your family is ready for SmartClinic</h2><p class="mt-2 text-slate-600">When you join a Healthy Families partner programme, its wellness schedule and eligible credits will appear here.</p></section>}@else{<div class="mt-8 grid gap-5 md:grid-cols-2">@for(p of programmes();track p.familyId){<section class="rounded-3xl border border-slate-200 bg-white p-6"><p class="text-sm font-bold text-brand-700">{{p.partner?.type}}</p><h2 class="mt-1 text-2xl font-black">{{p.partner?.name}}</h2><div class="mt-6 grid grid-cols-2 gap-4"><div class="rounded-2xl bg-slate-50 p-4"><p class="text-sm text-slate-500">Next wellness check-in</p><p class="mt-1 font-bold">{{p.nextWellnessAt?date(p.nextWellnessAt):'To be scheduled'}}</p></div><div class="rounded-2xl bg-slate-50 p-4"><p class="text-sm text-slate-500">Family Wellness Credit</p><p class="mt-1 text-xl font-black">{{money(p.wellnessCreditMinor,p.currency)}}</p></div></div></section>}</div>}<section class="mt-8 grid gap-3 sm:grid-cols-2 lg:grid-cols-3"><a routerLink="/providers" class="rounded-2xl bg-brand-700 p-5 font-bold text-white">Find Provider</a><a routerLink="/me/appointments" class="rounded-2xl border border-slate-200 p-5 font-bold">My Appointments</a><a routerLink="/me/health-records" class="rounded-2xl border border-slate-200 p-5 font-bold">Health Records</a></section></main>`,changeDetection:ChangeDetectionStrategy.OnPush})export class FamilyWellnessHomePageComponent{private api=inject(PartnerApiService);loading=signal(true);programmes=signal<any[]>([]);constructor(){this.api.familyHome().subscribe({next:x=>{this.programmes.set(x);this.loading.set(false);},error:()=>this.loading.set(false)});}date(x:string){return new Intl.DateTimeFormat('en-NG',{dateStyle:'medium'}).format(new Date(x));}money(x:string,c:string){return new Intl.NumberFormat('en-NG',{style:'currency',currency:c||'NGN'}).format(Number(x||0)/100);}}