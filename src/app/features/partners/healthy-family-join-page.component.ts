import { ChangeDetectionStrategy,Component,inject,signal } from '@angular/core';import { ActivatedRoute,Router } from '@angular/router';import { PartnerApiService } from '../../core/services/partner-api.service';@Component({selector:'app-healthy-family-join',template:`<main class="mx-auto max-w-2xl px-5 py-12"><p class="text-sm font-bold uppercase tracking-widest text-brand-700">Healthy Families · SmartClinic</p><h1 class="mt-3 text-4xl font-black text-slate-950">Connect your family to continuous care</h1><p class="mt-4 text-lg text-slate-600">Your school invited your family to SmartClinic. Your school can see programme participation, but not your diagnoses, consultations, tests, prescriptions or medical records.</p><section class="mt-8 rounded-3xl border border-slate-200 bg-white p-6"><h2 class="text-xl font-black">Before you join</h2><ul class="mt-4 space-y-3 text-slate-700"><li>✓ Your family's clinical records remain protected by SmartClinic access controls.</li><li>✓ You choose and manage authorised family/dependant relationships.</li><li>✓ Wellness reminders guide engagement; they do not force unnecessary tests.</li><li>✓ Programme benefits and wellness credits are for eligible healthcare use under programme rules.</li></ul><label class="mt-6 flex gap-3 rounded-2xl bg-slate-50 p-4"><input type="checkbox" #consent><span>I understand what SmartClinic processes and that my school receives only privacy-safe programme information unless I separately authorise more.</span></label><button type="button" [disabled]="!consent.checked||saving()" (click)="activate()" class="mt-5 w-full rounded-xl bg-brand-700 px-5 py-3 font-bold text-white disabled:opacity-40">{{saving()?'Activating…':'Join Healthy Families'}}</button>@if(message()){<p role="status" class="mt-4 font-semibold">{{message()}}</p>}</section></main>`,changeDetection:ChangeDetectionStrategy.OnPush})export class HealthyFamilyJoinPageComponent{private api=inject(PartnerApiService);private route=inject(ActivatedRoute);private router=inject(Router);saving=signal(false);message=signal('');activate(){const token=this.route.snapshot.paramMap.get('token')||'';this.saving.set(true);this.api.activate(token,'healthy-families-v1').subscribe({next:()=>{this.saving.set(false);this.message.set('Your family is connected. Opening My Family…');setTimeout(()=>this.router.navigateByUrl('/me/family'),500);},error:()=>{this.saving.set(false);this.message.set('We could not activate this invitation. Sign in first or ask the school for a fresh invitation.');}});}}