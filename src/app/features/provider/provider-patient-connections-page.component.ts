import{ChangeDetectionStrategy,Component,inject,signal}from'@angular/core';import{RouterLink}from'@angular/router';import{finalize}from'rxjs';import{ProviderPatientConnection}from'../../core/models/patient-provider-connection.model';import{PatientProviderConnectionsApiService}from'../../core/services/patient-provider-connections-api.service';
@Component({selector:'app-provider-patient-connections-page',imports:[RouterLink],changeDetection:ChangeDetectionStrategy.OnPush,template:`<main class="mx-auto max-w-7xl px-5 py-10 sm:px-8"><h1 class="text-3xl font-bold">Patient Connections</h1><p class="mt-2 text-slate-600">Process funded patient registration and identity-link requests.</p>@if(loading()){<p role="status" class="mt-8 rounded-2xl border p-6">Loading requests…</p>}@else if(error()){<div role="alert" class="mt-8 rounded-2xl bg-red-50 p-6">Unable to load requests. <button type="button" (click)="load()" class="font-bold underline">Try again</button></div>}@else if(!items().length){<section class="mt-8 rounded-2xl border p-8 text-center"><h2 class="text-xl font-bold">No Patient Connection requests</h2></section>}@else{<div class="mt-8 overflow-x-auto rounded-2xl border bg-white"><table class="min-w-full divide-y"><thead><tr>@for(h of ['Date','Patient','Request type','Current path','Funding','Status','Action'];track h){<th class="px-5 py-4 text-left text-xs font-bold uppercase text-slate-500">{{h}}</th>}</tr></thead><tbody class="divide-y">@for(c of items();track c.reference){<tr><td class="px-5 py-4">{{date(c.createdAt)}}</td><td class="px-5 py-4 font-bold">{{c.patient.givenName}} {{c.patient.familyName}}</td><td class="px-5 py-4">{{path(c.originalIntent)}}</td><td class="px-5 py-4">{{path(c.currentPath)}}</td><td class="px-5 py-4">{{funding(c)}}</td><td class="px-5 py-4">{{label(c.status)}}</td><td class="px-5 py-4"><a [routerLink]="['/provider/patient-connections',c.reference]" class="font-bold text-brand-700 underline">View</a></td></tr>}</tbody></table></div>}</main>`})
export class ProviderPatientConnectionsPageComponent{private readonly api=inject(PatientProviderConnectionsApiService);readonly items=signal<readonly ProviderPatientConnection[]>([]);readonly loading=signal(true);readonly error=signal(false);constructor(){this.load();}load(){this.loading.set(true);this.api.listProvider().pipe(finalize(()=>this.loading.set(false))).subscribe({next:p=>this.items.set(p.items),error:()=>this.error.set(true)});}funding(c:ProviderPatientConnection){return c.funding.every(x=>x.status==='PAID'||x.status==='SATISFIED_FREE')?'Satisfied':'Pending';}path(v:string){return v==='NEW_PATIENT_REGISTRATION'?'New registration':'Existing link';}label(v:string){return v.split('_').map(x=>x[0]+x.slice(1).toLowerCase()).join(' ');}date(v:string){return new Intl.DateTimeFormat('en-NG',{dateStyle:'medium'}).format(new Date(v));}}
