import{ProviderPayoutAccountStatus}from'../../core/models/provider-payout-account.model';export const payoutAccountStatusLabel=(s:ProviderPayoutAccountStatus)=>({PENDING_VERIFICATION:'Pending verification',VERIFIED:'Verified',DISABLED:'Disabled'}as const)[s];export const payoutAccountStatusHelp=(s:ProviderPayoutAccountStatus)=>({PENDING_VERIFICATION:'SmartClinic Operations has not yet verified this payout account.',VERIFIED:'This account has been reviewed and can be selected as a default payout destination.',DISABLED:'This account cannot be used for future payouts.'}as const)[s];export function payoutAccountEventLabel(v:string){const known:Record<string,string>={ACCOUNT_CREATED:'Account created',ACCOUNT_CORRECTED:'Account corrected',ACCOUNT_VERIFIED:'Account verified',ACCOUNT_SET_DEFAULT:'Set as default',ACCOUNT_DEFAULT_CLEARED:'Default cleared',ACCOUNT_DISABLED:'Account disabled'};return known[v]??v.toLowerCase().split('_').map(x=>x.charAt(0).toUpperCase()+x.slice(1)).join(' ')}
