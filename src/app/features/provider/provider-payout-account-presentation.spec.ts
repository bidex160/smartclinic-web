import{payoutAccountEventLabel,payoutAccountStatusHelp,payoutAccountStatusLabel}from'./provider-payout-account-presentation';describe('payout account presentation',()=>{it('uses exact safe status language',()=>{expect(payoutAccountStatusLabel('PENDING_VERIFICATION')).toBe('Pending verification');expect(payoutAccountStatusLabel('VERIFIED')).toBe('Verified');expect(payoutAccountStatusLabel('DISABLED')).toBe('Disabled');expect(payoutAccountStatusHelp('VERIFIED')).not.toContain('Paystack')});it('labels known audit events and safely formats future ones',()=>{expect(payoutAccountEventLabel('ACCOUNT_SET_DEFAULT')).toBe('Set as default');expect(payoutAccountEventLabel('FUTURE_EVENT')).toBe('Future Event')})});
