import { HttpErrorResponse } from '@angular/common/http';
import { ChangeDetectionStrategy, Component, inject, signal } from '@angular/core';
import { FormBuilder, ReactiveFormsModule } from '@angular/forms';
import { ActivatedRoute, RouterLink } from '@angular/router';
import { finalize, Observable } from 'rxjs';
import { AdminProviderPayoutDetail, ProviderPayoutDetail } from '../../core/models/provider-payout.model';
import { ProviderPayoutsApiService } from '../../core/services/provider-payouts-api.service';
import { earningSourceLabel, formatEarningMoney } from './provider-earning-presentation';
import { payoutMethodLabel, payoutStatusHelp, payoutStatusLabel } from './provider-payout-presentation';
type Action='PROCESS'|'COMPLETE'|'FAIL'|'CANCEL';

@Component({selector:'app-payout-detail-page',imports:[RouterLink,ReactiveFormsModule],changeDetection:ChangeDetectionStrategy.OnPush,template:`
<main class="mx-auto max-w-5xl px-5 py-10 sm:px-8"><a [routerLink]="admin?'/admin/provider-payouts':'/provider/payouts'" class="font-bold text-brand-700 underline">← Payouts</a>
@if(loading()){<p role="status" class="mt-6">Loading payout…</p>}@else if(error()){<p role="alert" class="mt-6 rounded-xl bg-red-50 p-5">This payout could not be loaded. <button (click)="load()" class="font-bold underline">Try again</button></p>}@else if(item();as p){
<header class="mt-6"><p class="break-all text-sm font-bold text-brand-700">{{p.reference}}</p><h1 class="mt-2 text-3xl font-bold">Payout details</h1><p class="mt-2"><strong>{{status(p.status)}}</strong> — {{statusHelp(p.status)}}</p></header>
<section class="mt-6 rounded-2xl border bg-white p-6">@if(admin){<div class="mb-5"><p>Provider</p><strong>{{provider(p).displayName}}</strong><p>{{provider(p).reference}}</p></div>}<dl class="grid gap-5 sm:grid-cols-2 lg:grid-cols-3"><div><dt>Total payout</dt><dd class="text-2xl font-bold">{{money(p.totalAmountMinor,p.currency)}}</dd></div><div><dt>Included earnings</dt><dd>{{p.earningCount}}</dd></div><div><dt>Settlement method</dt><dd>{{method(p.settlementMethod)}}</dd></div><div><dt>External settlement reference</dt><dd>{{p.externalReference||'Not recorded'}}</dd></div><div><dt>Created</dt><dd>{{date(p.createdAt)}}</dd></div>@if(p.processingAt){<div><dt>Processing started</dt><dd>{{date(p.processingAt)}}</dd></div>}@if(p.completedAt){<div><dt>Completed</dt><dd>{{date(p.completedAt)}}</dd></div>}@if(p.failedAt){<div><dt>Failed</dt><dd>{{date(p.failedAt)}}</dd></div>}@if(p.cancelledAt){<div><dt>Cancelled</dt><dd>{{date(p.cancelledAt)}}</dd></div>}@if(p.note){<div><dt>Operational note</dt><dd>{{p.note}}</dd></div>}</dl>
@if(admin){<div class="mt-6 flex flex-wrap gap-3">@if(p.status==='DRAFT'){<button (click)="open('PROCESS')" class="rounded-lg bg-brand-700 px-4 py-3 font-bold text-white">Start Processing</button><button (click)="open('CANCEL')" class="rounded-lg border px-4 py-3 font-bold">Cancel</button>}@if(p.status==='PROCESSING'){<button (click)="open('COMPLETE')" class="rounded-lg bg-brand-700 px-4 py-3 font-bold text-white">Complete Settlement</button><button (click)="open('FAIL')" class="rounded-lg border px-4 py-3 font-bold">Mark Failed</button><button (click)="open('CANCEL')" class="rounded-lg border px-4 py-3 font-bold">Cancel</button>}</div>}@else{<a routerLink="/provider/earnings" class="mt-6 inline-block font-bold text-brand-700 underline">View Earnings</a>}</section>
<section class="mt-6 rounded-2xl border bg-white p-6" [class.ring-2]="p.status==='PROCESSING'&&!!p.destination"><h2 class="text-xl font-bold">Recorded payout destination</h2>@if(p.destination;as d){<p class="mt-2 text-sm text-slate-600">Immutable destination snapshot recorded when this payout was created.</p><dl class="mt-4 grid gap-4 sm:grid-cols-2"><div><dt>Bank</dt><dd>{{d.bankName}} ({{d.bankCode}})</dd></div><div><dt>Account</dt><dd class="font-mono font-bold">{{d.maskedAccountNumber}}</dd></div><div><dt>Account name</dt><dd>{{d.accountName}}</dd></div><div><dt>Currency / Country</dt><dd>{{d.currency}} / {{d.countryCode}}</dd></div><div><dt>Payout-account reference</dt><dd class="break-all">{{d.payoutAccountReference}}</dd></div></dl>@if(admin&&p.status==='PROCESSING'){<p class="mt-4 rounded-lg bg-amber-50 p-3 text-sm">Only the masked account number is available here. SmartClinic does not expose the full account number through this payout response.</p>}}@else{<p class="mt-2 text-slate-600">Not recorded</p>}</section>
<section class="mt-6"><h2 class="text-xl font-bold">Included earnings</h2><div class="mt-3 overflow-x-auto rounded-xl border bg-white"><table class="w-full min-w-[680px] text-left"><thead><tr><th class="p-3">Reference</th><th class="p-3">Source</th><th class="p-3">Source reference</th><th class="p-3">Provider share</th><th class="p-3">Settled</th></tr></thead><tbody>@for(e of p.earnings;track e.reference){<tr class="border-t"><td class="p-3">{{e.reference}}</td><td class="p-3">{{source(e.sourceType)}}</td><td class="p-3">{{e.sourceReference}}</td><td class="p-3">{{money(e.providerShareMinor,e.currency)}}</td><td class="p-3">{{e.settledAt?date(e.settledAt):'Not settled'}}</td></tr>}</tbody></table></div></section>
<section class="mt-6"><h2 class="text-xl font-bold">Status history</h2><ol class="mt-3 grid gap-3">@for(h of p.history;track h.createdAt){<li class="rounded-xl border bg-white p-4"><strong>{{status(h.toStatus)}}</strong><span class="ml-2 text-sm">{{date(h.createdAt)}}</span><p>{{h.reasonNote||h.reasonCode}}</p></li>}</ol></section>}
@if(action();as a){<div class="fixed inset-0 z-50 flex items-center justify-center bg-black/50 p-4"><section role="alertdialog" aria-modal="true" class="w-full max-w-lg rounded-2xl bg-white p-6"><h2 class="text-2xl font-bold">{{actionTitle(a)}}</h2><p class="mt-3">{{actionHelp(a)}}</p><form [formGroup]="actionForm" (ngSubmit)="confirmAction()" class="mt-5 grid gap-4">@if(a==='COMPLETE'){<label>External settlement reference<input formControlName="externalReference" placeholder="e.g. bank transfer reference or settlement receipt number" class="mt-1 w-full rounded-lg border p-3"></label><label>Operational note (optional)<textarea formControlName="note" placeholder="e.g. Transfer confirmed by Finance on provider statement" class="mt-1 w-full rounded-lg border p-3"></textarea></label>}@if(a==='FAIL'||a==='CANCEL'){<label>Reason<textarea formControlName="reason" [placeholder]="a==='FAIL'?'e.g. Bank transfer rejected or settlement could not be confirmed':'e.g. Settlement cancelled for correction'" class="mt-1 w-full rounded-lg border p-3"></textarea></label>}@if(actionError()){<p role="alert">{{actionError()}}</p>}<div class="flex justify-end gap-3"><button type="button" (click)="close()" class="rounded-lg border px-4 py-2">Back</button><button [disabled]="invalid(a)||mutating()" class="rounded-lg bg-brand-700 px-4 py-2 font-bold text-white">{{mutating()?'Updating…':actionButton(a)}}</button></div></form></section></div>}</main>`})
export class PayoutDetailPageComponent{private readonly api=inject(ProviderPayoutsApiService);private readonly route=inject(ActivatedRoute);private readonly fb=inject(FormBuilder);readonly admin=this.route.snapshot.data['payoutScope']==='admin';private readonly reference=this.route.snapshot.paramMap.get('reference')??'';readonly item=signal<ProviderPayoutDetail|null>(null);readonly loading=signal(false);readonly error=signal(false);readonly action=signal<Action|null>(null);readonly mutating=signal(false);readonly actionError=signal('');readonly actionForm=this.fb.nonNullable.group({externalReference:'',note:'',reason:''});readonly money=formatEarningMoney;readonly status=payoutStatusLabel;readonly statusHelp=payoutStatusHelp;readonly method=payoutMethodLabel;readonly source=earningSourceLabel;constructor(){this.load()}load(){this.loading.set(true);this.error.set(false);const c=this.admin?this.api.getAdminPayout(this.reference):this.api.getProviderPayout(this.reference);c.pipe(finalize(()=>this.loading.set(false))).subscribe({next:v=>this.item.set(v),error:()=>this.error.set(true)})}open(a:Action){this.actionForm.reset();this.actionError.set('');this.action.set(a)}close(){if(!this.mutating())this.action.set(null)}invalid(a:Action){const f=this.actionForm.getRawValue();return a==='COMPLETE'?!f.externalReference.trim():a==='FAIL'||a==='CANCEL'?!f.reason.trim():false}confirmAction(){const a=this.action();if(!a||this.invalid(a)||this.mutating())return;const f=this.actionForm.getRawValue();let call:Observable<AdminProviderPayoutDetail>;if(a==='PROCESS')call=this.api.processPayout(this.reference);else if(a==='COMPLETE')call=this.api.completePayout(this.reference,{externalReference:f.externalReference.trim(),note:f.note.trim()||undefined});else if(a==='FAIL')call=this.api.failPayout(this.reference,{reason:f.reason.trim()});else call=this.api.cancelPayout(this.reference,{reason:f.reason.trim()});this.mutating.set(true);call.pipe(finalize(()=>this.mutating.set(false))).subscribe({next:v=>{this.item.set(v);this.action.set(null)},error:(e:HttpErrorResponse)=>{this.actionError.set(e.status===409?'This payout changed. The latest details have been reloaded.':'The payout could not be updated.');if(e.status===409)this.load()}})}provider(p:ProviderPayoutDetail){return(p as AdminProviderPayoutDetail).provider}date(v:string){return new Intl.DateTimeFormat('en-NG',{dateStyle:'medium',timeStyle:'short'}).format(new Date(v))}actionTitle(a:Action){return({PROCESS:'Start processing this payout?',COMPLETE:'Complete this settlement?',FAIL:'Mark this payout as failed?',CANCEL:'Cancel this payout?'}as const)[a]}actionButton(a:Action){return({PROCESS:'Start Processing',COMPLETE:'Complete Settlement',FAIL:'Mark Failed',CANCEL:'Cancel Payout'}as const)[a]}actionHelp(a:Action){return({PROCESS:'Starting processing does not mark the included earnings as settled.',COMPLETE:'Completing records settlement and changes included earnings from Payable to Settled.',FAIL:'Failed payouts do not settle included earnings.',CANCEL:'Cancelling releases earnings for a future payout.'}as const)[a]}}
