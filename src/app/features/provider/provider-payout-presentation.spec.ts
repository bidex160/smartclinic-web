import{payoutMethodLabel,payoutStatusHelp,payoutStatusLabel}from'./provider-payout-presentation';describe('Provider payout presentation',()=>{it('uses accurate manual settlement lifecycle language',()=>{expect(payoutStatusLabel('DRAFT')).toBe('Draft');expect(payoutStatusLabel('PROCESSING')).toBe('Processing');expect(payoutStatusLabel('COMPLETED')).toBe('Completed');expect(payoutStatusLabel('FAILED')).toBe('Failed');expect(payoutStatusLabel('CANCELLED')).toBe('Cancelled');expect(payoutStatusHelp('COMPLETED')).toContain('recorded as completed');expect(payoutStatusHelp('COMPLETED')).not.toContain('Money sent')});it('labels both confirmed manual methods',()=>{expect(payoutMethodLabel('MANUAL_BANK_TRANSFER')).toBe('Manual bank transfer');expect(payoutMethodLabel('MANUAL_OTHER')).toBe('Other manual settlement')})});
