import { ChangeDetectionStrategy, Component, computed, inject, signal } from '@angular/core';
import { RouterLink } from '@angular/router';
import { finalize } from 'rxjs';
import { ReferralImpact, ReferralTargetType } from '../../core/models/referral.model';
import { ReferralsApiService } from '../../core/services/referrals-api.service';

@Component({
  selector: 'app-my-impact-page', imports: [RouterLink], changeDetection: ChangeDetectionStrategy.OnPush,
  template: `<main class="mx-auto max-w-7xl px-5 py-10 sm:px-8 lg:py-14"><header><p class="text-sm font-bold uppercase tracking-wider text-brand-600">Patient Portal</p><h1 class="mt-2 text-3xl font-bold text-brand-900">My Impact</h1><p class="mt-2 max-w-3xl text-slate-600">See the people and care providers you've connected to SmartClinic. Only verified, qualified referrals count toward progression.</p></header>
  @if (loading()) { <div role="status" aria-label="Loading your impact" class="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">@for (item of [1,2,3,4]; track item) { <div class="h-32 animate-pulse rounded-2xl bg-slate-100"></div> }</div> }
  @if (error()) { <section role="alert" class="mt-8 rounded-2xl border border-red-200 bg-red-50 p-6 text-red-900"><h2 class="font-bold">We couldn't load your impact right now.</h2><button type="button" (click)="load()" class="mt-4 rounded-lg border border-red-300 px-4 py-2 font-bold">Try again</button></section> }
  @if (impact(); as value) {
    <section class="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4" aria-label="Impact summary"><article class="rounded-2xl border bg-white p-5"><p class="font-semibold text-slate-600">Lifetime points earned</p><p class="mt-2 text-3xl font-bold text-brand-900">{{ value.balances.lifetimeEarnedPoints }}</p></article><article class="rounded-2xl border bg-white p-5"><p class="font-semibold text-slate-600">Qualified referrals</p><p class="mt-2 text-3xl font-bold text-brand-900">{{ value.summary.qualifiedReferrals }}</p></article><article class="rounded-2xl border bg-white p-5"><p class="font-semibold text-slate-600">Current level</p><p class="mt-2 text-2xl font-bold text-brand-900">{{ value.levelProgress.currentLevel?.name ?? 'No level yet' }}</p></article><article class="rounded-2xl border bg-white p-5"><p class="font-semibold text-slate-600">Leaderboard position</p><p class="mt-2 text-2xl font-bold text-brand-900">{{ leaderboardLabel(value) }}</p></article></section>

    <div class="mt-8 grid gap-6 lg:grid-cols-3"><section class="rounded-2xl border bg-white p-6"><h2 class="text-xl font-bold text-brand-900">Your referral code</h2><p class="mt-3 font-mono text-2xl font-bold">{{ value.referralCode }}</p><button type="button" (click)="copyCode()" class="mt-4 min-h-11 rounded-lg bg-brand-700 px-4 font-bold text-white focus:ring-4 focus:ring-brand-200">Copy code</button><p aria-live="polite" class="mt-2 text-sm font-semibold text-brand-700">{{ copyFeedback() }}</p><p class="mt-3 text-sm text-slate-600">This code identifies your referrals. It is not an account password or access credential.</p></section>
    <section class="rounded-2xl border bg-white p-6 lg:col-span-2"><h2 class="text-xl font-bold text-brand-900">Reward balances</h2><div class="mt-4 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><div><p class="text-sm text-slate-600">Available</p><p class="text-2xl font-bold">{{ value.balances.availablePoints }}</p></div><div><p class="text-sm text-slate-600">Reserved</p><p class="text-2xl font-bold">{{ value.balances.reservedPoints }}</p></div><div><p class="text-sm text-slate-600">Lifetime earned</p><p class="text-2xl font-bold">{{ value.balances.lifetimeEarnedPoints }}</p></div><div><p class="text-sm text-slate-600">Lifetime redeemed</p><p class="text-2xl font-bold">{{ value.balances.lifetimeRedeemedPoints }}</p></div></div><p class="mt-4 text-sm text-slate-600">Available points can be used for eligible Smart Health Checks. Reserved points are held for pending reward operations; lifetime earned points are a historical total.</p><a routerLink="/me/referrals" class="mt-4 inline-flex font-bold text-brand-700 underline">Manage rewards and withdrawals</a></section></div>

    <section class="mt-8" aria-labelledby="invite-heading"><h2 id="invite-heading" class="text-2xl font-bold text-brand-900">Invite someone</h2><div class="mt-4 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">@for (target of inviteTargets(); track target) { <article class="rounded-2xl border bg-white p-5"><h3 class="font-bold">Invite a {{ inviteLabel(target) }}</h3><div class="mt-4 flex flex-wrap gap-2"><button type="button" (click)="copyInvite(target)" class="min-h-11 rounded-lg bg-brand-700 px-4 font-bold text-white">Copy invite link</button>@if (canShare) { <button type="button" (click)="shareInvite(target)" class="min-h-11 rounded-lg border border-brand-700 px-4 font-bold text-brand-700">Share</button> }</div></article> }</div><p aria-live="polite" class="mt-3 font-semibold text-brand-700">{{ inviteFeedback() }}</p></section>

    <section class="mt-8 rounded-2xl border bg-white p-6" aria-labelledby="progress-heading"><h2 id="progress-heading" class="text-2xl font-bold text-brand-900">Referral progression</h2>@if (value.levelProgress.currentLevel; as current) { <p class="mt-3 text-xl font-bold">{{ current.name }} achieved</p> } @else { <p class="mt-3 text-xl font-bold">No level achieved yet</p>@if (value.levelProgress.nextLevel; as first) { <p class="mt-1 text-slate-600">Complete the first referral level requirements to reach {{ first.name }}.</p> } }@if (value.levelProgress.highestConfiguredLevelReached) { <p class="mt-4 rounded-xl bg-brand-50 p-4 font-semibold text-brand-900">Highest referral level achieved</p><p class="mt-3 text-slate-600">Keep growing verified healthcare connections.</p> } @else if (value.levelProgress.nextLevel; as next) { <h3 class="mt-5 text-lg font-bold">Next: {{ next.name }}</h3><div class="mt-4 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">@for (requirement of value.levelProgress.requirements; track requirement.targetType) { <article class="rounded-xl bg-slate-50 p-4"><h4 class="font-bold">{{ targetLabel(requirement.targetType) }}</h4><p class="mt-2 text-xl font-bold">{{ requirement.qualified }} / {{ requirement.required }}</p><p class="mt-1 text-sm">{{ requirement.qualified }} of {{ requirement.required }} qualified</p><p class="mt-2 font-semibold">{{ requirement.completed ? 'Completed' : requirement.remaining + ' remaining' }}</p></article> }</div><p class="mt-5 text-slate-600">Invite more people or care providers to progress toward {{ next.name }}.</p> }</section>

    <div class="mt-8 grid gap-6 lg:grid-cols-2"><section class="rounded-2xl border bg-white p-6"><h2 class="text-xl font-bold text-brand-900">Verified connections</h2><div class="mt-4 grid grid-cols-2 gap-4">@for (target of targets; track target) { <div><p class="text-sm text-slate-600">{{ targetLabel(target) }}</p><p class="text-2xl font-bold">{{ value.qualifiedCounts[target] }}</p></div> }</div></section><section class="rounded-2xl border bg-white p-6"><h2 class="text-xl font-bold text-brand-900">Referral pipeline</h2><div class="mt-4 grid grid-cols-3 gap-3"><div><p class="text-sm text-slate-600">Registered</p><p class="text-2xl font-bold">{{ value.summary.registeredReferrals }}</p></div><div><p class="text-sm text-slate-600">Qualified</p><p class="text-2xl font-bold">{{ value.summary.qualifiedReferrals }}</p></div><div><p class="text-sm text-slate-600">Pending</p><p class="text-2xl font-bold">{{ value.summary.pendingReferrals }}</p></div></div><p class="mt-4 text-sm text-slate-600">Registered referrals joined through your invitation. Qualified referrals met SmartClinic requirements; pending referrals have not qualified yet.</p></section></div>

    <section class="mt-8 rounded-2xl border bg-white p-6" aria-labelledby="leaderboard-heading"><h2 id="leaderboard-heading" class="text-xl font-bold text-brand-900">Public leaderboard</h2><p class="mt-2 text-slate-600">Users appear publicly only when they choose to participate. Rankings use verified referral impact and backend-authoritative earned points.</p><p class="mt-4 font-semibold">Current state: {{ value.leaderboard.optedIn ? 'Participating' : 'Not participating' }}</p>@if (value.leaderboard.optedIn) { <p class="mt-1">{{ value.leaderboard.position ? 'Your current position: #' + value.leaderboard.position : "You're participating, but you're not ranked yet." }}</p> }<button type="button" (click)="updateLeaderboard(!value.leaderboard.optedIn)" [disabled]="preferenceUpdating()" class="mt-5 min-h-11 rounded-lg border border-brand-700 px-4 font-bold text-brand-700 disabled:opacity-50">{{ preferenceUpdating() ? 'Updating preference…' : value.leaderboard.optedIn ? 'Leave public leaderboard' : 'Join public leaderboard' }}</button><p aria-live="polite" class="mt-3 text-sm font-semibold" [class.text-red-800]="preferenceError()">{{ preferenceFeedback() }}</p></section>

    <section class="mt-8 flex flex-wrap items-center justify-between gap-4 rounded-2xl bg-brand-900 p-6 text-white"><div><h2 class="text-xl font-bold">Referral activity</h2><p class="mt-1 text-brand-100">Review your direct referral history and reward operations.</p></div><a routerLink="/me/referrals" class="rounded-lg bg-white px-5 py-3 font-bold text-brand-900">View referral history</a></section>
  }
  </main>`,
})
export class MyImpactPageComponent {
  private readonly api=inject(ReferralsApiService);readonly impact=signal<ReferralImpact|null>(null);readonly loading=signal(false);readonly error=signal(false);readonly preferenceUpdating=signal(false);readonly preferenceError=signal(false);readonly preferenceFeedback=signal('');readonly copyFeedback=signal('');readonly inviteFeedback=signal('');readonly canShare=typeof navigator.share==='function';readonly targets:ReferralTargetType[]=['PATIENT','CLINIC','LABORATORY','PHARMACY'];readonly inviteTargets=computed(()=>{const links=this.impact()?.inviteLinks;if(!links)return[];return this.targets.filter(target=>Boolean(links[target]))});
  constructor(){this.load()} load(){if(this.loading())return;this.loading.set(true);this.error.set(false);this.api.getMyImpact().pipe(finalize(()=>this.loading.set(false))).subscribe({next:value=>this.impact.set(value),error:()=>this.error.set(true)})}
  leaderboardLabel(value:ReferralImpact){return !value.leaderboard.optedIn?'Not participating':value.leaderboard.position?`#${value.leaderboard.position}`:'Not ranked yet'} targetLabel(target:ReferralTargetType){return({PATIENT:'Patients',CLINIC:'Clinics',LABORATORY:'Laboratories',PHARMACY:'Pharmacies'}as const)[target]} inviteLabel(target:ReferralTargetType){return({PATIENT:'patient',CLINIC:'clinic',LABORATORY:'laboratory',PHARMACY:'pharmacy'}as const)[target]}
  async copyCode(){const code=this.impact()?.referralCode;if(!code)return;try{await navigator.clipboard.writeText(code);this.copyFeedback.set('Copied')}catch{this.copyFeedback.set('Copy was unavailable. Select the code and copy it manually.')}} async copyInvite(target:ReferralTargetType){const link=this.impact()?.inviteLinks[target];if(!link)return;try{await navigator.clipboard.writeText(link);this.inviteFeedback.set(`${this.targetLabel(target)} invite link copied.`)}catch{this.inviteFeedback.set('Copy was unavailable. Try again.')}} async shareInvite(target:ReferralTargetType){const link=this.impact()?.inviteLinks[target];if(link&&navigator.share)await navigator.share({title:`SmartClinic ${this.targetLabel(target)} invitation`,text:'Join SmartClinic using my referral link.',url:link}).catch(()=>undefined)}
  updateLeaderboard(publicLeaderboard:boolean){if(this.preferenceUpdating())return;this.preferenceUpdating.set(true);this.preferenceError.set(false);this.preferenceFeedback.set('');this.api.updateLeaderboardPreference(publicLeaderboard).subscribe({next:()=>{this.preferenceUpdating.set(false);this.preferenceFeedback.set(publicLeaderboard?'You are now participating in the public leaderboard.':'You have left the public leaderboard.');this.reloadImpact()},error:()=>{this.preferenceUpdating.set(false);this.preferenceError.set(true);this.preferenceFeedback.set('We could not update your leaderboard preference. Try again.')}})} private reloadImpact(){this.api.getMyImpact().subscribe({next:value=>this.impact.set(value),error:()=>{this.preferenceError.set(true);this.preferenceFeedback.set('Preference saved, but we could not refresh your position yet.')}})}
}
