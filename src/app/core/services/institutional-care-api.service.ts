import { HttpClient,HttpParams } from '@angular/common/http';import { inject,Injectable } from '@angular/core';import { Observable } from 'rxjs';import { API_CONFIG } from '../config/api-config.token';import { InstitutionalCareDetail,InstitutionalCareSummary } from '../models/institutional-care.model';
@Injectable({providedIn:'root'}) export class InstitutionalCareApiService{private readonly http=inject(HttpClient);private readonly base=inject(API_CONFIG).baseUrl;list(q=''):Observable<readonly InstitutionalCareSummary[]>{let params=new HttpParams();if(q.trim())params=params.set('q',q.trim());return this.http.get<readonly InstitutionalCareSummary[]>(`${this.base}/public/institutions`,{params});}detail(reference:string):Observable<InstitutionalCareDetail>{return this.http.get<InstitutionalCareDetail>(`${this.base}/public/institutions/${encodeURIComponent(reference)}`);}}